import React from 'react';
import styles from './Feed.module.css';

const Feed = (props) => {
  return (
    <div className={styles.feed}>
      <span>Feed</span>
    </div>
  );
};

export default Feed;
